import { Box, Container, Typography, Stack } from '@mui/material'

const SocialCirle = () => {
    return (
        <Box flex={4} p={2} sx={{ width: '100%', height: '95vh' }} >
            <Typography>CIRCLE</Typography>
        </Box>
    );
}

export default SocialCirle;